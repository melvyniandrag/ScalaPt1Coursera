package test

object test {
  val a = new sub()
  println(a.toString())
}

abstract class base {
  def foo = 1
  def bar : Int
}

class sub extends base {
  override def foo = 2
  def bar = 2
  override def toString = "{" + bar + "}"
}
